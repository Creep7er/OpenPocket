# Input

Read controls only through `PocketInput`: directions, A/B/X/Y, MENU, BACK, and `axis()`. Every screen must work with console controls, including settings and confirmation dialogs. Direct `Input` calls and mouse-only navigation are not allowed.
