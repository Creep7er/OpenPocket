# Theme

Use `PocketTheme.palette()` for `dark`, `mid`, `light`, and `hi` colors. Use hard pixel edges, nearest filtering, integer coordinates, and PixelFont. Avoid gradients, blur, antialiased arcs, default Godot fonts, and mouse-only widgets.
