# Updating

Use the same package id and a higher semantic version. Package-scoped settings and data survive installation and app-only removal. Downgrades are identified separately and require explicit confirmation.

Godot cannot safely unload mounted resource packs in this runtime. Updating a cartridge already launched in the current process shows `RESTART POPUGVPOCKET TO APPLY UPDATE`.
